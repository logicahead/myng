import {AssetInformationService} from "./asset-information.service";

describe('AssetInformationService', () => {
    let assetService:AssetInformationService;
    beforeEach(() => assetService = new AssetInformationService(null, null));

    describe('Asset Information Service', () => {
        // it('should exist', () => {
        //     expect(assetService).toBeTruthy();
        // });
    });
});
